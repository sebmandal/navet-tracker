from setuptools import setup

setup(
    name="navet-tracker",
    version="0.0",
    description="A simple script that tracks the number of open positions for events on ifinavet.no and notifies a Discord webhook of the number of open positions",
    author="Sebastian Mandal",
    author_email="sebastian@mandal.group",
    packages=[""],
    install_requires=[""],
)
